# API
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
httpx>=0.28.1

# LLM
google-genai==1.64.0

# LiveKit — all three must share the same minor version
livekit-agents==1.4.3
livekit-plugins-google==1.4.3
livekit-plugins-silero==1.4.3
# NOTE: do NOT pin livekit separately — livekit-agents manages it as a transitive dep

# Database
sqlalchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.9

# Celery + Redis
celery==5.4.0
redis==5.1.1

# Utils
pydantic[email]==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
